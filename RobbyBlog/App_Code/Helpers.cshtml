@helper Tags(System.Web.Mvc.HtmlHelper htmlHelper, IList<RobbyBlog.Core.Objects.Tag> tags)
{
  foreach (var tag in tags)
  {
    <div class="tags-div">@RobbyBlog.ActionLinkExtensions.TagLink(htmlHelper, tag)</div>     
  }
}